name: CI

on: 
  push:
    tags:
      - '20.*'
  schedule:
    - cron: '37 * * * *'

jobs:
  
  yo:
    runs-on: ubuntu-latest
    steps:
    - run: echo lol
    - run: echo "${{ toJson(github) }}"
     
  version:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
    - run: echo lol
    - run: echo "${{ toJson(github) }}"
    - run: echo "::set-output name=action_fruit::strawberry"
  build:
    runs-on: ubuntu-latest
    needs: version
    if: jobs.version.output.version
    steps:
    - uses: actions/checkout@v2
    - name: Save cash
      run: |
        echo 2
